---
title: "PII Redaction"
description: "Guide to PII detection and redaction in Aptly"
---

# PII Redaction Guide

Aptly automatically detects and redacts Personally Identifiable Information (PII) from your requests before sending them to LLM providers.

## How It Works

Every request to `/v1/chat/completions` goes through PII detection and redaction:

1. **Detection**: Aptly scans your input messages using Microsoft Presidio
2. **Redaction**: PII is replaced according to your configured mode
3. **LLM Call**: The redacted messages are sent to the LLM provider

## Redaction Modes

Aptly supports three redaction modes:

- **mask** (default): `"John Smith"` → `"PERSON_A"`
- **hash**: `"John Smith"` → `"HASH_a3f2c1b9"`
- **remove**: `"John Smith"` → `"[REDACTED]"`
